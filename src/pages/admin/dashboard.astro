---
/*
 * There is one dashboard, and it is /admin.
 *
 * This page existed briefly as a second one, which meant the portal had two
 * pages both called Dashboard showing overlapping counts. The analytics moved
 * to /admin, where the owner already lands; this stays only so a bookmark or a
 * link from that period still arrives somewhere sensible.
 */
return Astro.redirect('/admin', 301);
---
