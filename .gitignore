node_modules/
dist/
.astro/
.wrangler/
.dev.vars
.env
.DS_Store

# Book covers live in R2, not the repo. Rebuild them locally with
# `node scripts/fetch-images.mjs`, then push with `node scripts/upload-covers.mjs`.
public/img/books/
.scratch/

# Working files for scripts/standardise-covers.mjs - derived, never source.
.cache/
